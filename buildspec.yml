
version: 0.2

phases:
   install:
      commands:
         - echo Entering install phase...
         - echo Nothing to do now
   build:
      commands:
         - echo Build started on `date`
         - mvn install
artifacts:
   files:
      - target/auction.jar
   discard-paths: yes
