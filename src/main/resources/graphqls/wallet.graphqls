extend type Query {
    getBalances : [BalancePayload]

}

extend type Mutation {
    getDepositAddress(
        currency: String!
    ) : String
    withdrawCrypto(
        to: String,
        amount: Float,
        tokenSymbol: String
    ) : Boolean
    depositWithStripe(
        amount: Float,
        currencyName: String,
        paymentIntentId: String,
        paymentMethodId: String
    ) : PayResponse
}

schema {
    query: Query
}