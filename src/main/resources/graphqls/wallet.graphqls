extend type Query {
    getDepositAddress(
        id: String,
        token: String,
        network: String
    ) : String
}

# extend type Mutation {

# }

schema {
    query: Query,
    # mutation: Mutation
}