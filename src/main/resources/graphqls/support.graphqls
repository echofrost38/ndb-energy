extend type Mutation {
    unknownMemoRecovery(
        coin: String,
        receiverAddr: String,
        depositAmount: Float,
        txId: String
    ) : String
    requestPhone2FA(
        phone: String
    ) : String
    confirmPhone2FA(
        code: String
    ) : String
}

schema {
    mutation: Mutation
}