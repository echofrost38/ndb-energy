extend type Query {
    getInternalAddress(
        id: String,
        token: String,
        network: String
    ) : String
    getWalletBalance(
        token: String,
        network: String,
        address: String
    ) : String
}

extend type Mutation {
    transferFunds(
        token: String,
        network: String,
        address: String,
        amount: Int
    ) : Boolean
}

schema {
    query: Query,
    # mutation: Mutation
}