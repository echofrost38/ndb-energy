extend type Query {
    getBalances : [Balance]

}

extend type Mutation {
    getDepositAddress(
        currency: String!
    ) : String
    withdrawCrypto(
        to: String,
        amount: Float,
        tokenSymbol: String
    ) : Boolean
}

schema {
    query: Query
}